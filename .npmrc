; vite-react-ssg declares react-router-dom ^6 as an optional peer, but the app
; runs react-router 7 (which still re-exports the router APIs it uses). Allow the
; install to proceed so Netlify's `npm install` doesn't fail on the peer range.
legacy-peer-deps=true
